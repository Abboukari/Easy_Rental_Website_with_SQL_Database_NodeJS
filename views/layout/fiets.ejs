<!DOCTYPE html>
<html lang="nl" dir="ltr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="Welkom op de verhuurpagina van biker haaglanden" />
    <link rel="stylesheet" href="https://use.typekit.net/vqt1jaf.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"
    />
    <link rel="stylesheet" href="./css/style.css" />
    <title>Fietsen</title>
  </head>

  <body>
    <header>
      <%- include('./partials/navigation') %> 
    </header>
    <section class="logo12">
      <a href="./index.html" target="_blank"
        ><img
          src="./images/Biker.com main background .png"
          alt="logo van biker haaglanden"
          width="1300"
          height="400"
      /></a>
    </section>
    <%- include(`../pages/${template}`) %> 
    <!-- krijg de tabel hieronder niet zicht baar?  Als ik hem in de pages file stop dan krijg een 404 error-->
    <% if (fiets.length) { %> 
      <table  class="med-tafel">
        <thead>
          <tr>
            <th scope="col">framenummer</th>
            <th scope="col">Type fiets</th>
            <th scope="col">Model fiets</th>
            <th scope="col">Dagprijs</th>
            <th scope="col">Nieuwprijs</th>
            <th scope="col">Merk</th>
          </tr>
        </thead>
          <tbody>
            <% for(var i = 0; i < data.length; i++) { %>
              <tr>
                <th scope="row"><%= (i+1) %></th>
                <td><%= data.[i].framenummer %></td>
                <td><%= data.[i].type_fiets %></td>
                <td><%= data.[i].modelfiets %></td>
                <td><%= data.[i].dagprijs %></td>
                <td><%= data.[i].borg %></td>
                <td><%= data.[i].nieuwprijs %></td>
                <td><%= data.[i].merkfiets %></td>
              </tr>
           <% } %>
          </tbody>
      </table>
      <% }  %> 

    <footer>
      <%- include('./partials/footer') %>
    </footer>
  </body>
</html>
